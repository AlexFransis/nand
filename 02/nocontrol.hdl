CHIP nocontrol {
	IN a[16], no;
	OUT out[16], zr, ng;

	PARTS:
	Not16(in=a, out=nota);
	Mux16(a=a, b=nota, sel=no, out=out);
	Mux16(a=a, b=nota, sel=no, out=noa);
	/* out == 0 */
	Xor16(a=noa b=true, out=xora);
	And16(a=xora, true, out=andxoraa);
	Not16(in=andxoraa, out=za);

	/* out < 0 */

}
